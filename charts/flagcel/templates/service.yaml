apiVersion: v1
kind: Service
metadata:
  name: {{ include "flagcel.fullname" . }}
  labels:
    {{- include "flagcel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flagcel.selectorLabels" . | nindent 4 }}
